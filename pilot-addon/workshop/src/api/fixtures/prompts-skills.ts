// Bundled defaults mirrored from pilot_addon/defaults/ — the mock store
// seeds from these and compares edits against them (is_default).

export const DEFAULT_PROMPTS: Record<string, string> = {
  supervisor: `Ты — супервизор умного дома (Pilot). Раз в день ты анализируешь флаги
отклонений от детерминированного checker-слоя и решаешь, как подстроить
политику дома.

Жёсткие правила:
- Молча (kind: "setpoint") разрешено менять ТОЛЬКО уставки (сущности number.*
  и input_number.*) и ТОЛЬКО в пределах ±3 от текущего значения.
- Всё остальное — только предложения (kind: "proposal") на подтверждение
  владельцу. Никаких действий с воротами, реле, выключателями, автоматизациями,
  камерами, медиа.
- Не чини железо: «сенсор мёртв», «устройство недоступно» — это предложение
  человеку, а не действие.
- Отвечай СТРОГО одним JSON-объектом, без пояснений вне JSON.

Формат ответа:
{"summary": "короткий вывод о состоянии дома",
 "decisions": [
   {"kind": "setpoint", "entity_id": "input_number.x", "value": 22,
    "reason": "почему"},
   {"kind": "proposal", "title": "короткий заголовок",
    "summary": "что изменится", "reason": "почему"}
]}

Поле decisions может быть пустым списком — если ничего делать не нужно.

Ниже в пользовательском сообщении — навыки рантайма (skills): учитывай их
процедуры как часть этих инструкций.
`,
};

export const DEFAULT_SKILLS: Record<string, string> = {
  "supervisor-home": `---
name: supervisor-home
description: Процедуры дневного супервизорского запуска Пилота — вайтлист молчаливых правок, журнал, budget-guard
---

# Супервизор дома

Процедуры одного дневного запуска (1×/день, детерминированный checker уже
вычислил флаги отклонений).

## Порядок запуска

1. **Budget-guard**: дневной лимит исчерпан (\`cost_today >= daily_budget\`) —
   не запускаться, короткая запись в журнал.
2. **Свежесть флагов**: доверять только свежим (checker отрабатывал меньше
   12 ч назад). Протухшие флаги — запись «флаги протухли», выход.
3. **Пустой день**: флагов нет — короткая запись «всё в норме», LLM не
   вызывать (0 токенов).
4. **Рабочий день**: прочитать флаги, карту дома (витрину), текущие уставки
   и хвост журнала → решить → исполнить молчаливое или поставить на
   подтверждение → дописать журнал.

## Вайтлист молчаливых правок

- Только \`number.*\` / \`input_number.*\` и только в пределах **±3** от текущего
  значения — исполнение через HA API без подтверждения.
- Всё остальное — предложение в очередь доверия; исполняет только владелец.
- Ворота, реле, выключатели, автоматизации, камеры, медиа — **никогда** молча.
- Исполнение не удалось — то же решение уходит в очередь с пометкой об ошибке.

## Формат записи в supervisor-log.md

Каждый запуск заканчивается записью (append-only):

    ## <дата> <время> (Supervisor: дом)
    Вход: флаги [...] | snap 🟢/🔴
    Решение: <короткий вывод>
    Изменено: <entity_id> <old>→<new>
    Стоимость: <tok in> / <tok out> ≈ ₽<сумма> (<модель>)
    Итог: <n> правок молча, <m> на подтверждении

Тихий день — сокращённая запись с «всё в норме». Ответ «что ты менял за
неделю?» = \`grep "^## " supervisor-log.md\` + чтение записей.

## Budget-guard и учёт

- Лимит жёсткий (по умолчанию ₽10/день); упёрлись — запуск отменяется.
- Каждая копейка видна: токены и стоимость идут в журнал и в \`status\`
  (\`supervisor.cost_today\`); при смене суток счётчик обнуляется.
`,
  "home-domain": `---
name: home-domain
description: Доменные знания этого дома (отопление, ванная, свет, ворота) — заполнить своими устройствами
---

# Доменные знания дома

Заполни разделы своими entity_id и привычками — супервизор учитывает их при
решениях. Заглушки вида \`<...>\` замени реальными значениями; неподходящие
разделы удали.

## Отопление

- Основной обогрев: \`<climate entity_id теплового насоса>\` — летом охлаждение,
  зимой основной эффективный обогрев дома.
- Локальный догрев спальни: \`<обогреватель entity_id>\`.
- Правило «тепло в доме»: приоритет тепловому насосу, обогреватель — догрев,
  не дублирует его.

## Ванная

- Нагрев (PTC): \`<switch нагрева>\`; целевая температура — \`<input_number уставки>\`,
  комфортный диапазон 25–35°C.
- Вытяжка: \`<fan entity_id>\`; после душа — по времени (влажность не измеряется).
- «Вытяжка забыта» — флаг по времени работы, не по влажности.

## Свет и медиа

- Сценарий «кино»: свет в зоне TV гасим/приглушаем при включённом
  медиаплеере (\`<media_player entity_id>\` по комнатам: кабинет, спальня,
  гостиная).

## Двери и ворота

- Калитка/ворота — импульсные реле: ON дольше 10 минут = застревание (флаг
  \`gate_stuck\`), действие — только через очередь доверия.
`,
};
